name: Euronext Press Release Scraper

on:
  schedule:
    # Run every 5 minutes during extended hours (6 AM - 11:59 PM Norwegian time)  
    # Note: GitHub Actions uses UTC time, Norway is UTC+1/+2
    - cron: '*/5 4-21 * * *'  # Every 5 minutes, covers 6 AM - 11:59 PM Norwegian
    
  # Allow manual triggering
  workflow_dispatch:
    inputs:
      test_mode:
        description: 'Run in test mode (limited results)'
        required: false
        default: 'false'
        type: boolean

jobs:
  scrape-and-update:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
          
    - name: Check if within scheduled hours
      id: check_time
      run: |
        current_hour=$(date +%H)
        echo "Current hour (UTC): $current_hour"
        
        # Convert UTC to Norwegian time (CET/CEST)
        norwegian_hour=$(TZ='Europe/Oslo' date +%H)
        echo "Norwegian hour: $norwegian_hour"
        
        # Check if between 6 AM and 11:59 PM Norwegian time
        if [ $norwegian_hour -ge 6 ] && [ $norwegian_hour -lt 24 ]; then
          echo "within_hours=true" >> $GITHUB_OUTPUT
          echo "Within scheduled hours (6 AM - 11:59 PM Norwegian time)"
        else
          echo "within_hours=false" >> $GITHUB_OUTPUT
          echo "Outside scheduled hours"
        fi
        
    - name: Run scraper
      if: steps.check_time.outputs.within_hours == 'true' || github.event_name == 'workflow_dispatch'
      env:
        WEBFLOW_API_TOKEN: ${{ secrets.WEBFLOW_API_TOKEN }}
        WEBFLOW_SITE_ID: ${{ secrets.WEBFLOW_SITE_ID }}
        WEBFLOW_COLLECTION_ID: ${{ secrets.WEBFLOW_COLLECTION_ID }}
        TEST_MODE: ${{ github.event.inputs.test_mode || 'false' }}
      run: |
        if [ "$TEST_MODE" = "true" ]; then
          echo "Running in test mode"
          node src/index.js --test
        else
          echo "Running in production mode"
          node src/index.js
        fi
        
    - name: Upload logs on failure
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: scraper-logs-${{ github.run_id }}
        path: |
          *.log
          data/processed.json
        retention-days: 7
        
    - name: Commit updated processed.json
      if: success()
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        
        if [ -n "$(git status --porcelain data/processed.json)" ]; then
          git add data/processed.json
          git commit -m "Update processed releases data [skip ci]"
          git push
        else
          echo "No changes to commit"
        fi
        
    - name: Notify on failure
      if: failure()
      run: |
        echo "::error::Scraper failed to run successfully. Check the logs for details."